  const { scrollY } = useScroll({ container: scrollRef });
  const topMaskOpacity = useTransform(scrollY, [0, 24], [1, 0]);
  const maskImage = useMotionTemplate`linear-gradient(to bottom, rgba(0,0,0,${topMaskOpacity}), black 12px, black 100%)`;
